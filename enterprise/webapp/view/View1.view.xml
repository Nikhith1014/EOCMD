<mvc:View
    controllerName="enterprise.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:viz="sap.viz.ui5.controls"
    xmlns:vizData="sap.viz.ui5.data"
    xmlns:vizFeeds="sap.viz.ui5.controls.common.feeds"
    displayBlock="true">

    <App>
        <pages>
            <Page showHeader="false">

                <!-- 📌 APPLICATION TITLE (ONLY ONE) -->
                <Title
                    text="{i18n>appTitle}"
                    level="H2"
                    class="pageHeader"
                />

                <!-- 📌 MAIN CONTENT -->
                <VBox class="dashboardPage">

                    <!-- 📌 KPI CARDS SECTION -->
                    <l:Grid width="100%" defaultSpan="L4 M6 S12" class="kpiRow">

                        <VBox class="kpiCard">
                            <core:Icon src="sap-icon://group" size="3rem" class="kpiIcon" />
                            <Text text="Total Customers" class="kpiLabel" />
                            <Text text="{dashboard>/totalCustomers}" class="kpiValue" />
                        </VBox>

                        <VBox class="kpiCard">
                            <core:Icon src="sap-icon://sales-order" size="3rem" class="kpiIcon" />
                            <Text text="Total Orders" class="kpiLabel" />
                            <Text text="{dashboard>/totalOrders}" class="kpiValue" />
                        </VBox>

                        <VBox class="kpiCard">
                            <core:Icon src="sap-icon://pending" size="3rem" class="kpiIcon" />
                            <Text text="Pending Orders" class="kpiLabel" />
                            <Text text="{dashboard>/pendingOrders}" class="kpiValue" />
                        </VBox>

                    </l:Grid>

                    <!-- 📌 CHARTS SECTION -->
                    <l:Grid width="100%" defaultSpan="L6 M12 S12" class="chartRow">

                        <!-- Bar Chart -->
                        <VBox class="chartCard">
                            <Title text="Orders Per Customer" />
                            <viz:VizFrame id="barChart" vizType="column" height="350px" width="100%">
                                <viz:dataset>
                                    <vizData:FlattenedDataset data="{dashboard>/ordersPerCustomer}">
                                        <vizData:dimensions>
                                            <vizData:DimensionDefinition name="Customer" value="{name}" />
                                        </vizData:dimensions>
                                        <vizData:measures>
                                            <vizData:MeasureDefinition name="Orders" value="{orders}" />
                                        </vizData:measures>
                                    </vizData:FlattenedDataset>
                                </viz:dataset>
                                <viz:feeds>
                                    <vizFeeds:FeedItem uid="categoryAxis" type="Dimension" values="Customer" />
                                    <vizFeeds:FeedItem uid="valueAxis" type="Measure" values="Orders" />
                                </viz:feeds>
                            </viz:VizFrame>
                        </VBox>

                        <!-- Donut Chart -->
                        <VBox class="chartCard">
                            <Title text="Order Status Overview" />
                            <viz:VizFrame id="donutChart" vizType="donut" height="350px" width="100%">
                                <viz:dataset>
                                    <vizData:FlattenedDataset data="{dashboard>/orderStatus}">
                                        <vizData:dimensions>
                                            <vizData:DimensionDefinition name="Status" value="{status}" />
                                        </vizData:dimensions>
                                        <vizData:measures>
                                            <vizData:MeasureDefinition name="Count" value="{count}" />
                                        </vizData:measures>
                                    </vizData:FlattenedDataset>
                                </viz:dataset>
                                <viz:feeds>
                                    <vizFeeds:FeedItem uid="size" type="Measure" values="Count" />
                                    <vizFeeds:FeedItem uid="color" type="Dimension" values="Status" />
                                </viz:feeds>
                            </viz:VizFrame>
                        </VBox>

                    </l:Grid>

                </VBox>
            </Page>
        </pages>
    </App>
</mvc:View>
