_schema-version: "3.2"
ID: EOCMD
description: Enterprise Order & Customer Management Dashboard
version: 0.0.1
modules:
- name: EOCMD-router
  type: approuter.nodejs
  path: router
  requires:
  - name: EOCMD-html5-repo-runtime
  - name: EOCMD-uaa
  - name: EOCMD-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: EOCMD-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: EOCMD-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - enterprise.zip
      name: enterprise
      target-path: resources/
- name: enterprise
  type: html5
  path: enterprise
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: EOCMD-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: EOCMD-${space-guid}
    service: xsuaa
    service-plan: application
- name: EOCMD-html5-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: EOCMD-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: EOCMD-destination-service
    service-plan: lite
- name: EOCMD-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: EOCMD-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
